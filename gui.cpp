#include "game.h"
#include "include.h"
/*
void Game::text(int x, int y, string _text){

    // -- Cargar la fuente -- //
    fnt_Surface = TTF_RenderText_Solid(fnt, _text.c_str(), fnt_Color);

    // -- Definicion de parametros -- //
    fnt_Pos.x = x;
    fnt_Pos.y = y;
    fnt_Pos.w = fnt_Surface->w;
    fnt_Pos.h = fnt_Surface->h;

    // -- Draw -- //
    fnt_Texture = SDL_CreateTextureFromSurface(ren, fnt_Surface);
    SDL_FreeSurface(fnt_Surface);
    SDL_RenderCopy(ren, fnt_Texture, NULL, &fnt_Pos);

}
*/
